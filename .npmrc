# .npmrc configuration for keyboard-shortcuts-lite
# Compatible with both npm and Bun package managers

# Registry configuration
# Default to npm, but can be overridden for bucket systems
registry=https://registry.npmjs.org

# Bucket system registry (commented out - uncomment to use)
# registry=https://your-bucket.com/packages

# Scope-specific registry configuration
# Public organization scopes
@brendadeeznuts1111:registry=https://registry.npmjs.org
@keyboard-shortcuts:registry=https://registry.npmjs.org

# Private enterprise registries (examples - uncomment to use)
# @myorg:registry=https://registry.my-company.com/
# @internal:registry=https://npm.internal.company.com/
# @enterprise:registry=https://enterprise-registry.com/

# Development registries
@dev:registry=http://localhost:4873/
@test:registry=http://localhost:4873/

# Bucket system scope (when available)
# @bucket:registry=https://bucket.company.com/packages/

# Authentication for private registries
# Replace with your actual token or use environment variables
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}
# //your-bucket.com/:_authToken=${BUCKET_TOKEN}

# Authentication for scoped registries
# //registry.my-company.com/:_authToken=${ENTERPRISE_NPM_TOKEN}
# //npm.internal.company.com/:_authToken=${INTERNAL_NPM_TOKEN}
# //enterprise-registry.com/:_authToken=${ENTERPRISE_TOKEN}
# //localhost:4873/:_authToken=${DEV_NPM_TOKEN}

# Alternative auth methods
# //your-bucket.com/:username=myusername
# //your-bucket.com/:_password=${BASE64_ENCODED_PASSWORD}
# //your-bucket.com/:_auth=${BASE64_ENCODED_USERNAME_PASSWORD}

# Installation configuration
save-exact=false
link-workspace-packages=true

# Performance optimization
cache=true

# Security settings
ignore-scripts=false
dry-run=false

# Dependency management
# omit=dev
# include=dev,peer

# Installation strategy (compatible with npm, pnpm, yarn)
# install-strategy=hoisted
# node-linker=hoisted

# Hoisting patterns
# public-hoist-pattern=*eslint*
# hoist-pattern=*

# Custom CA certificates (if needed for private registries)
# cafile=/path/to/ca-bundle.crt

# Environment-specific overrides
# These can be overridden by environment variables or bunfig.toml
